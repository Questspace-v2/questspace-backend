db:
  hosts:
    - postgres
  port: 5432
  database: postgres
  user: env:POSTGRES_USER
  password: env:POSTGRES_PASSWORD
cors:
  allow-origins:
    - "*"
hash-cost: 11
jwt:
  secret: env:JWT_SECRET_KEY